<div id="BrandLogo"></div>
<div class="content">
    <div class="section">
        <p>Welcome to {{brand}} {{user}}.</p>

        <p>Your identity provider has issued the following tokens.</p>
    </div>

    <div class="section">
        <h1>ID Token</h1>
        <p>The ID token describes you as a user to this application.</p>
        <pre>{{jwt idtoken}}</pre>
    </div>

    <div class="section">
        <h1>Access Token</h1>
        <p>The access token describes which resources this application has access to
        based on your identity and the grants your have provided.</p>
        <pre>{{jwt accesstoken}}</pre>
    </div>

    {{#if refreshtoken}}
        <div class="section">
        <h1>Refresh Token</h1>
        <p>This token allows this application to get further access tokens.</p>
        <pre>{{refreshtoken}}</pre>
    </div>
    {{/if}}

    {{#if customLink}}
    <div class="section">
        <a href="{{customLink}}">{{customLinkText}}</a>
    </div>
    {{/if}}


    {{#if authority}}
    <div class="section">
        <h1>Exercise authority</h1>
        <p>You can assume the identity of the following users.</p>
        <form action="/authority" method="post">
            <select id="identity" name="identity" >
            {{#each authority}}
                <option value="{{this.id}}">{{this.displayName}}</option>
            {{/each}}
            </select>
            <button type="submit">
                Go
            </button>
        </form>
    </div>
    {{/if}}

    <div class="section">
        <a href="/logout">Logout</a>
    </div>
</div>